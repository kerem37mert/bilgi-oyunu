import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 20,
        flexDirection: "row",
        justifyContent: "space-between"
    },
    returnText: {
        fontSize: 20,
        fontWeight: "bold",
        color: "#fff"
    },
    endText: {
        fontSize: 25,
        fontWeight: "bold",
        color: "#fff"
    }
});