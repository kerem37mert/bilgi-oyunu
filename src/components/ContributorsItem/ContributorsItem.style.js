import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        marginTop: 15
    },
    text: {
        fontSize: 16,
        color: "#fff",
        textAlign: "center",
        fontWeight: "600"
    }
});
