import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: "#1C82AD",
        padding: 20,
        borderRadius: 20,
        marginTop: 20
    },
    text: {
        color: "#fff",
        fontSize: 16
    }
});